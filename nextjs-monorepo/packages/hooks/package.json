{
  "name": "@myapp/hooks",
  "version": "1.0.0",
  "main": "dist/index.js",
  "author": "Rob Wilson",
  "license": "MIT",
  "scripts": {
    "dev": "yarn run clean && yarn run watch",
    "build": "yarn run clean && yarn run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "react": "17.0.2"
  },
  "devDependencies": {
    "@types/react": "17.0.14",
    "tsc": "^2.0.3",
    "typescript": "^4.3.5"
  }
}
